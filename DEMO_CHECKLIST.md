# 시연 당일 체크리스트

데모데이 진행 순서와 막혔을 때 대처법. 위에서부터 순서대로 하면 됩니다.

설치가 처음이라면 [README.md](README.md)를 먼저 보세요. 이 문서는 **이미 설치가
끝난 PC에서 시연 당일에 하는 일**만 담습니다.

---

## 1. 시작 30분 전

### 1.1 서버 띄우기 (터미널 1)

```bash
cd server
```

```bash
.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

> **`--reload`를 붙이지 마세요.** 파일이 바뀔 때마다 재시작되고, 재시작하면
> 모델을 다시 올리느라 그 사이 요청이 전부 느려지거나 끊깁니다.

### 1.2 판정기 준비 확인 — **이게 가장 중요합니다**

서버가 뜬 뒤 모델을 백그라운드로 올립니다. 다 올라오기 전에 시연을 시작하면
첫 판정이 6초 넘게 걸려 앱이 멈춘 것처럼 보입니다.

```bash
curl http://localhost:8000/health
```

```json
{ "status": "ok", "ready": true,
  "model": { "fresh": true, "processed": true, "ocr": "easyocr", "supported_items": 33 } }
```

**`ready`가 `true`가 될 때까지 기다렸다가 시작하세요.** 보통 10초 안에 됩니다.
터미널에도 `[catalog_ai] 예열 완료` 한 줄이 찍힙니다.

`ready`가 계속 `false`거나 `processed`가 `false`면 아래를 실행해 원인을 봅니다.

```bash
.venv\Scripts\python.exe check_model.py
```

### 1.3 폰 연결 (터미널 2)

Tailscale이 **PC와 폰 양쪽 다** 켜져 있는지 먼저 확인하세요.

```bash
$env:REACT_NATIVE_PACKAGER_HOSTNAME="<PC의 Tailscale IP>"
```

```bash
cd app
```

```bash
npx expo start --lan
```

QR을 찍어 앱이 뜨면, **홈 화면이 뜨는지**까지 확인하세요. 홈이 뜨면 폰이 서버에
닿은 것입니다(최근 등록 목록을 서버에서 받아옵니다).

### 1.4 폰 설정

| 항목 | 이유 |
|---|---|
| 저전력 모드 끄기 | 카메라·네트워크가 느려집니다 |
| 자동 잠금 '안 함' | 시연 중 화면이 꺼지면 Expo Go가 재연결합니다 |
| 방해금지 모드 켜기 | 알림 배너가 촬영 화면을 가립니다 |
| 밝기 최대 | 심사위원이 화면을 같이 봅니다 |
| **Expo Go 자동 업데이트 꺼짐 확인** | SDK 55로 올라가면 프로젝트가 안 열립니다 |

### 1.5 한 번 미리 돌려보기

**본 시연 전에 반드시 한 바퀴 돌려보세요.** 이때 등록한 화물은 이력에 남으니
시연용과 구분되는 이름이면 좋습니다(출발지를 다르게 주면 됩니다).

---

## 2. 시연 시나리오

### 2.1 가공품 — 표시사항으로 세번 찾기 (메인)

준비물: **인쇄한 표시사항**(원재료명과 함량이 또렷한 것). 포장 실물이 있으면 더 좋습니다.

| 단계 | 화면 | 보여줄 것 |
|---|---|---|
| 1 | 홈 → 시작하기 | |
| 2 | 화물 종류 **가공품** 선택, 출발지 입력 | 상품명을 **안 받는다**는 점. "무엇을 보내는지는 사진이 말한다" |
| 3 | 운송장 발급 | 화물마다 다른 마커 패턴 (체적 측정의 기준자) |
| 4 | 표시사항 촬영 | |
| 5 | 성분 결과 | OCR이 읽은 원재료·함량. 틀린 값은 이 화면에서 고칠 수 있음 |
| 6 | **HS Code 결과** | **여기가 핵심.** 코드와 함께 판정 근거를 읽어주세요 |
| 7 | 박스 촬영 → 체적 | |
| 8 | 등록 확인 → 공동물류 신청 | 출발지에 따라 집하 권역이 갈립니다 |
| 9 | 집하 → 혼재 → 출고·정산 | 절감액 |

**6단계에서 읽어줄 문장** — 떡갈비 표시사항 기준입니다.

> HS 제16류 주1 — 육류 함량 80%가 20%를 초과하므로 제16류(육류 조제품)로 분류함

이름만 보면 '떡'갈비라 과자류(제19류)로 갑니다. 원재료 함량을 읽어야 조제육류가
됩니다. **이것이 표시사항을 찍는 이유**이고, 이 앱이 검색기가 아닌 이유입니다.

### 2.2 원물 — 사진으로 품목 알아내기

준비물: 딸기·배·수박·무화과·멜론 중 하나(실물 또는 선명한 사진).

| 단계 | 보여줄 것 |
|---|---|
| 화물 종류 **원물** 선택 | 원물은 표시사항이 없으므로 OCR을 아예 돌리지 않습니다 |
| 상품 촬영 | |
| HS Code 결과 | **차순위 후보에 상태별 코드가 나옵니다** |

**상태 선택이 이 화면의 볼거리입니다.** 신선 딸기와 냉동 딸기는 같은 사진이지만
세번이 다릅니다(0810.10 / 0811.10). 사진으로 알 수 없는 것은 **추측하지 않고
사용자에게 묻습니다.** 후보를 눌러 냉동으로 바꿔 보이세요.

### 2.3 실패 대응 — 물어보면 보여줄 것

설정 탭 → **프로필 영역(농/전남 농원)을 0.7초 길게 누르면** 시연 설정이 열립니다.

| 보여줄 상황 | 설정 | 앱의 반응 |
|---|---|---|
| 표시사항을 못 읽음 | HS Code 강제 결과 → 표시사항 흐림 | 직접 입력 화면으로 자동 전환 |
| 함량이 안 적혀 있음 | → 함량 없음 | 판정은 하되 '사전심사 권장'으로 강등 |
| 각도가 낮음 | 체적 강제 결과 → 각도 미달 | "조금 더 위에서 찍어주세요" |
| 모서리가 가림 | → 모서리 가림 | 모서리 직접 지정 화면 |

**판정 불가는 강제하지 않아도 볼 수 있습니다.** 지원하지 않는 품목(예: 커피, 라면)을
찍으면 코드를 지어내지 않고 판정 불가로 내려갑니다.

> 잘못된 세번으로 신고하면 통관이 거부되어 화물이 폐기됩니다.
> 그래서 확신이 없으면 추측하지 않고 직접 입력이나 사전심사로 안내합니다.

---

## 3. 막혔을 때

| 증상 | 원인 | 대처 |
|---|---|---|
| 첫 판정이 유난히 느리다 | 예열 전에 시작함 | 기다리면 됩니다. 다음부터는 정상 속도 |
| 모든 화물이 '판정 불가' | 모델이 안 올라옴 | `check_model.py` 실행. 터미널에 `가공식품 판정기 없음`이 찍혔는지 확인 |
| 가공품만 판정 불가 | 가공식품 판정기만 죽음 | 서버 재시작. `/health`의 `processed` 확인 |
| 앱이 "서버에 연결할 수 없습니다" | Tailscale 끊김 | 양쪽 Tailscale 확인 → 설정 탭 숨김 메뉴에서 서버 주소 직접 입력 |
| 홈 화면이 비어 있다 | 서버 미연결 | 위와 같음 |
| 카메라가 안 열린다 | 권한 거부 | 각 촬영 화면의 [직접 입력하기]로 우회. 시연은 계속 진행됩니다 |
| 품목을 못 알아본다 | 실사진 인식 한계 | **직접 입력으로 우회.** 이건 알려진 한계이고, 대체 경로가 있다는 게 설계 의도입니다 |
| Expo Go가 프로젝트를 못 연다 | SDK 버전 불일치 | 되돌릴 방법이 없습니다. 폰의 Expo Go 버전이 54.0.x인지 확인 |

**무엇이 잘못돼도 앱은 멈추지 않습니다.** 모든 AI 단계에 직접 입력 경로가 있습니다.
막히면 당황하지 말고 직접 입력으로 넘어가면서 "AI가 실패해도 신고 준비는 계속된다"고
설명하는 편이 오히려 설계 의도를 보여줍니다.

---

## 4. 하지 말 것

- `--reload`로 서버 띄우기
- 시연 중 서버 파일 수정 (재시작되면 모델을 다시 올립니다)
- `server/shipda.db` 삭제 (이력이 비면 홈·이력 화면이 허전합니다)
- Expo Go 업데이트
- 시연 직전 `npm install` / `pip install`

---

## 5. 알려진 한계 — 물어보면 솔직하게

숨기는 것보다 아는 대로 답하는 편이 낫습니다. 심사위원은 대개 한계를 아는지를 봅니다.

| 질문 | 답 |
|---|---|
| 체적도 AI가 재나요? | **아닙니다.** 체적은 고정값을 돌려주는 목업입니다. 상자 자동 인식은 실물 환경에서 정확도가 안 나와 철회했고, 그 경위를 기능명세서 변경 이력 5에 남겼습니다 |
| 품목 인식 정확도는? | 공개 데이터셋 기준 Top-1 87.4%, Top-2 94.9%(5품목 1,583장). 다만 **폰 실촬영은 배경·조명이 달라 더 낮습니다.** 그래서 확신 없으면 후보를 제시하고 사용자가 고르게 합니다 |
| 몇 품목이나 되나요? | 원물은 학습한 33개 품목, 가공품은 관세율표 2,100행 전체를 검색 대상으로 씁니다 |
| 틀리면 어떻게 되나요? | 통관 거부·화물 폐기로 이어집니다. 그래서 확신에 찬 오답을 가장 위험하게 보고, 판정 불가로 내려 사전심사를 안내합니다 |
| 사진은 저장되나요? | 판정에 쓴 뒤 지웁니다. 서버에 남기지 않습니다 |

---

## 6. 되돌리기

시연 직전에 무언가 꼬였다면 마지막으로 정상 동작한 커밋으로 돌아갈 수 있습니다.

```bash
git log --oneline -10
```

```bash
git stash
```

작업 중이던 변경을 치워두고 마지막 커밋 상태로 돌아갑니다. `git stash pop`으로
되살릴 수 있습니다. **시연 당일에는 새로 고치지 말고 되돌리는 쪽을 택하세요.**
