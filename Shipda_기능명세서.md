# Shipda 앱 기능명세서

## 1. 서비스 개요

**Shipda(쉽다)**는 수출 경험이 없는 영세 농가·소상공인이 관세사나 물류업체 없이, 앱만으로 수출 신고 준비(HS Code 판정, 화물 체적 측정)를 할 수 있게 돕는 모바일 앱이다.

- **타겟 사용자**: 광주·전남 지역의 수출 초보 영세 농가/상인
- **타겟 품목**: 1차 가공품 1종 (예: 유자 착즙/건조 가공품) — 실제 서비스에서는 이 품목명을 상수(constant)로 관리하며, 다른 품목으로 교체 가능한 구조로 설계한다
- **핵심 기능**: ① HS Code 자동 판정, ② 화물 체적(CBM) 자동 측정
- **설계 원칙**: HS Code 판정과 CBM 측정은 서로 독립적인 모듈이다. 한쪽이 실패하거나 신뢰도가 낮아도, 사용자가 해당 항목만 수동으로 입력하고 나머지 흐름은 그대로 진행할 수 있어야 한다.

---

## 2. 전체 화면 구성 및 네비게이션

```
[메인 화면]
    │ "상품 분석 시작하기" 버튼
    ▼
[화면 1] 상품 정보 입력
    │ "다음" 버튼
    ▼
[화면 2] 성분표 촬영 → HS Code 판정
    │ 정상 인식 시: "확인" 버튼 → 화면 3
    │ 인식 실패/저신뢰 시: "직접 입력" 화면으로 분기 → 입력 완료 후 화면 3
    ▼
[화면 3] 박스 촬영 → CBM 측정
    │ 정상 인식 시: "확인" 버튼 → 화면 4
    │ 인식 실패 시: "직접 입력" 화면으로 분기 → 입력 완료 후 화면 4
    ▼
[화면 4] 결과 요약
    │ "수정하러 가기" → 화면 2 또는 화면 3으로 되돌아가기
    │ "등록 완료" 버튼 → 화면 5
    ▼
[화면 5] 등록 완료

[메인 화면 하단 네비게이션 바 — 항상 노출]
    ├─ [홈 아이콘] → 메인 화면
    ├─ [시계 아이콘] → 분석 이력 화면
    └─ [사람 아이콘] → 설정 화면
```

---

## 3. 화면별 상세 명세

### 3.1 메인 화면

**목적**: 서비스 진입점, 새 상품 등록 시작 및 최근 등록 상품 확인

**UI 구성 요소**
| 요소 | 타입 | 설명 |
|---|---|---|
| 서비스 로고/타이틀 | 텍스트 | "쉽다" + 서브타이틀 "수출을 쉽고 빠르게" |
| 상품 분석 시작하기 버튼 | 버튼(primary) | 클릭 시 화면 1로 이동 |
| 최근 등록 상품 리스트 | 리스트(카드형, 최대 5개) | 각 카드: 상품명, 등록일, 상태 뱃지(진행중/완료). 클릭 시 화면 4(결과 요약) 형태로 상세 조회 |
| 하단 네비게이션 바 | 탭바(3개 아이콘) | 홈 / 이력 / 설정 |

**API 호출**
- 화면 진입 시: `GET /product/recent?limit=5` (최근 등록 상품 리스트 조회)

---

### 3.2 화면 1. 상품 정보 입력

**목적**: 신규 상품 등록을 위한 최소 필수 정보 입력

**UI 구성 요소**
| 요소 | 타입 | 필수 여부 | 설명 |
|---|---|---|---|
| 뒤로가기 버튼 | 아이콘 버튼 | - | 메인 화면으로 이동 |
| 상품명 입력창 | 텍스트 입력 | 필수 | placeholder: "상품명을 입력하세요" |
| 다음 버튼 | 버튼(primary) | - | 상품명 미입력 시 비활성화(disabled) |

**유효성 검사**
- 상품명은 공백만 입력된 경우 제출 불가, "상품명을 입력해주세요" 인라인 에러 메시지 표시

**사용자 액션**
- [다음] 클릭 → `product_id` 발급받아 화면 2로 이동

**API 호출**
```
POST /product/create
Request Body:
{
  "product_name": "string"
}
Response:
{
  "product_id": "string (uuid)",
  "created_at": "ISO8601 datetime"
}
```

---

### 3.3 화면 2. 성분표 촬영 → HS Code 판정

**목적**: 원재료 성분표 이미지를 분석해 HS Code를 자동 판정

**UI 구성 요소 (촬영 단계)**
| 요소 | 타입 | 설명 |
|---|---|---|
| 뒤로가기 버튼 | 아이콘 버튼 | 화면 1로 이동 |
| 카메라 뷰 / 이미지 업로드 영역 | 카메라 컴포넌트 | 촬영 또는 갤러리에서 이미지 선택 가능 |
| 촬영/업로드 버튼 | 버튼 | 이미지 캡처 |
| 분석 시작 버튼 | 버튼(primary) | 이미지 업로드 완료 후 활성화 |

**UI 구성 요소 (분석 중 상태)**
| 요소 | 타입 | 설명 |
|---|---|---|
| 로딩 스피너 | 애니메이션 | "AI가 분석하고 있어요" 텍스트와 함께 표시 |
| 진행 단계 텍스트 | 텍스트(순차 전환) | "사진 인식 중" → "HS Code 후보 탐색 중" |

**UI 구성 요소 (결과 표시 — 정상 인식)**
| 요소 | 타입 | 설명 |
|---|---|---|
| 인식된 원재료 목록 | 리스트(수정 가능) | 각 항목: 원재료명, 함량(%). 항목별 [수정] 아이콘 클릭 시 인라인 편집 가능 |
| 식품유형 | 텍스트(수정 가능) | 예: "과채가공품" |
| 보관방법 | 텍스트(수정 가능) | 예: "냉장" |
| HS Code 결과 카드 | 카드 | HS Code(10자리, "0000.00-0000" 형식으로 표기), 판정 상태 뱃지, 신뢰도(%) |
| 판정 상태 뱃지 | 뱃지(3종) | `신고 가능`(녹색) / `사전심사 권장`(주황) / `검토 필요`(빨강) |
| 판정 근거 텍스트 | 텍스트 | 예: "OO류 주1 — 특정 성분 함량이 기준을 초과하여 해당 분류로 판정함" |
| 차순위 후보 리스트 | 리스트(최대 3개) | 각 항목: HS Code, 품목명, 신뢰도(%) |
| 확인 버튼 | 버튼(primary) | 클릭 시 화면 3으로 이동, 판정 결과 저장 |
| 사진 다시 촬영 버튼 | 버튼(secondary) | 촬영 단계로 복귀 |

**UI 구성 요소 (결과 표시 — 신뢰도 낮음/인식 실패)**
| 요소 | 타입 | 설명 |
|---|---|---|
| 경고 배너 | 배너(주황/빨강) | "신뢰도가 낮아요(N%) 사진을 다시 확인해주세요" |
| 가능성 있는 후보 리스트 | 리스트(최대 3개, 신뢰도 표시) | 사용자가 직접 후보 중 하나를 선택 가능 |
| 사진 다시 촬영 버튼 | 버튼(primary) | 촬영 단계로 복귀 |
| 직접 입력하기 버튼 | 버튼(secondary) | 아래 "직접 입력 화면"으로 이동 |

**직접 입력 화면 (예외 처리 경로)**
| 요소 | 타입 | 설명 |
|---|---|---|
| 원재료명 입력 | 텍스트 입력(복수 추가 가능) | + 버튼으로 원재료 행 추가 |
| 함량(%) 입력 | 숫자 입력 | 각 원재료별 함량, 합계가 100%인지 실시간 검증 |
| 식품유형 입력 | 텍스트 입력 | - |
| 보관방법 선택 | 드롭다운 | 상온/냉장/냉동 |
| 저장 버튼 | 버튼(primary) | 입력 완료 → HS Code는 자동 판정 없이 "검토 필요" 상태로 저장, 화면 3으로 이동 |

**예외 처리 규칙**
| 상황 | 시스템 반응 |
|---|---|
| 성분표 이미지가 흐려 텍스트 인식 실패 | "직접 입력 화면"으로 자동 전환 안내 |
| 원재료 함량 정보가 이미지에 없음 | HS Code는 산출하되 판정 상태를 "검토 필요"로 강제 표시 |
| 동일 신뢰도의 후보가 여러 개 | 판정 상태를 "사전심사 권장"으로 표시, 근거 텍스트에 사유 명시 |

**API 호출**
```
POST /product/{product_id}/hscode/predict
Request: multipart/form-data
  - image: file

Response:
{
  "ingredients": [
    {"name": "string", "ratio": "number (0-100)"}
  ],
  "food_type": "string",
  "storage_method": "string",
  "hscode_result": {
    "hs_code": "string (10자리, 예: 2005991000)",
    "status": "approved | pre_review_recommended | review_required",
    "confidence": "number (0-1)",
    "reasoning": "string",
    "alternatives": [
      {"hs_code": "string", "name": "string", "confidence": "number"}
    ]
  }
}
```

```
POST /product/{product_id}/hscode/manual
Request Body:
{
  "ingredients": [
    {"name": "string", "ratio": "number"}
  ],
  "food_type": "string",
  "storage_method": "string"
}
Response:
{
  "hscode_result": {
    "hs_code": null,
    "status": "review_required",
    "confidence": null,
    "reasoning": "사용자 직접 입력 - AI 판정 미수행"
  }
}
```

---

### 3.4 화면 3. 박스 촬영 → CBM 측정

**목적**: 화물 박스 사진으로 가로·세로·높이·체적을 자동 산출

**사전 안내**
- 이 화면 진입 전, 크기 기준 마커(기준자 역할을 하는 인쇄물)를 박스 표면에 부착하도록 안내하는 서브 화면을 먼저 노출한다.
- 마커는 상품 등록 시 `product_id`에 연동되어 자동 생성되며, 사용자가 출력해서 사용한다.

**UI 구성 요소 (마커 안내 서브 화면)**
| 요소 | 타입 | 설명 |
|---|---|---|
| 마커 미리보기 | 이미지 | 출력할 마커 이미지 |
| 출력하기 버튼 | 버튼 | 인쇄 또는 이미지 저장 |
| 다음 버튼 | 버튼(primary) | 촬영 단계로 이동 |

**UI 구성 요소 (촬영 단계)**
| 요소 | 타입 | 설명 |
|---|---|---|
| 카메라 뷰 | 카메라 컴포넌트 | 실시간 프리뷰 |
| 각도 안내 가이드(수평계) | 오버레이 UI | 촬영 각도가 30~40도 범위에 들어오면 초록색으로 변경, 벗어나면 빨간색<br>**[변경 2026-08-11] 30~40도 → 60~70도** (변경 이력 1 참고) |
| 마커 인식 표시 | 오버레이 | 마커 인식 시 테두리 하이라이트<br>**[변경 2026-08-11] 촬영 화면에서는 검출된 상자 영역을 초록 사각형으로 표시** (변경 이력 3 참고)<br>**[철회 2026-08-11] 상자 인식 제거, 각도 프레임만 표시** (변경 이력 5 참고) |
| 촬영 버튼 | 버튼(primary) | 각도 조건 충족 시에만 활성화<br>**[변경 2026-08-11] 각도 조건 + 상자 인식이 모두 충족돼야 활성화** (변경 이력 2 참고)<br>**[철회 2026-08-11] 원래대로 각도 조건만으로 활성화** (변경 이력 5 참고) |

**UI 구성 요소 (결과 표시 — 정상 측정)**
| 요소 | 타입 | 설명 |
|---|---|---|
| 촬영 이미지 미리보기 | 이미지 | 박스 모서리에 윤곽선 오버레이 표시 |
| 치수 결과 | 텍스트(3개 값) | 가로(mm) / 세로(mm) / 높이(mm) |
| 체적 결과 | 텍스트 | CBM 단위로 표시 |
| 무게 입력창 | 숫자 입력 | 필수 입력 (AI로 측정 불가, 사용자 직접 입력) |
| 다시 촬영 버튼 | 버튼(secondary) | 촬영 단계로 복귀 |
| 확인 버튼 | 버튼(primary) | 무게 입력 완료 시 활성화, 클릭 시 화면 4로 이동 |

**예외 처리 규칙**
| 상황 | 시스템 반응 |
|---|---|
| 마커를 인식하지 못함 | "운송장이 화면에 다 들어오게 찍어주세요" 안내 + 재촬영 유도 |
| 마커가 너무 작게 촬영됨 | "조금 더 가까이서 찍어주세요" 안내 + 재촬영 유도 |
| 촬영 각도가 30도 미만 | "조금 더 위에서 찍어주세요" 안내 + 재촬영 유도<br>**[변경 2026-08-11] 기준 각도 30도 → 60도** (변경 이력 1 참고) |
| 박스 모서리 일부가 가려짐 | 사용자가 화면에서 네 모서리를 손가락으로 직접 지정하는 수동 보정 모드로 전환 |
| 3회 이상 연속 실패 | "직접 입력하기" 버튼을 강조 표시하여 수동 입력 경로로 유도 |

**직접 입력 화면 (예외 처리 경로)**
| 요소 | 타입 | 설명 |
|---|---|---|
| 가로/세로/높이 입력 | 숫자 입력(mm 단위, 3개) | - |
| 무게 입력 | 숫자 입력(kg 단위) | - |
| 저장 버튼 | 버튼(primary) | 입력값으로 CBM 자동 계산 후 저장, 화면 4로 이동 |

**API 호출**
```
GET /product/{product_id}/marker
Response:
{
  "marker_image_url": "string (URL)"
}
```

```
POST /product/{product_id}/cbm/predict
Request: multipart/form-data
  - image: file
  - device_angle: number (기기 각도 센서값, degree)

Response:
{
  "success": "boolean",
  "dimensions": {
    "width_mm": "number",
    "depth_mm": "number",
    "height_mm": "number"
  },
  "cbm": "number",
  "error_reason": "marker_not_found | marker_too_small | angle_invalid | edge_occluded | null"
}
```

```
POST /product/{product_id}/cbm/manual
Request Body:
{
  "width_mm": "number",
  "depth_mm": "number",
  "height_mm": "number",
  "weight_kg": "number"
}
Response:
{
  "cbm": "number (자동 계산: width * depth * height / 1,000,000,000)"
}
```

---

### 3.5 화면 4. 결과 요약

**목적**: HS Code 판정 결과와 CBM 측정 결과를 한 화면에서 통합 확인

**UI 구성 요소**
| 요소 | 타입 | 설명 |
|---|---|---|
| 뒤로가기 버튼 | 아이콘 버튼 | 화면 3으로 이동 |
| 상품 정보 카드 | 카드 | 상품명 |
| HS Code 결과 카드 | 카드 | HS Code, 판정 상태 뱃지, [수정하러 가기] 버튼 → 화면 2로 이동 |
| CBM 결과 카드 | 카드 | 가로/세로/높이/체적/무게, [수정하러 가기] 버튼 → 화면 3으로 이동 |
| 등록 완료 버튼 | 버튼(primary) | 클릭 시 화면 5로 이동 |

**API 호출**
```
GET /product/{product_id}/summary
Response:
{
  "product_name": "string",
  "hscode_result": { ... 3.3의 hscode_result 스키마 동일 ... },
  "cbm_result": {
    "width_mm": "number",
    "depth_mm": "number",
    "height_mm": "number",
    "cbm": "number",
    "weight_kg": "number"
  }
}
```

---

### 3.6 화면 5. 등록 완료

**목적**: 등록 완료 안내

**UI 구성 요소**
| 요소 | 타입 | 설명 |
|---|---|---|
| 완료 아이콘 | 아이콘 | 체크 표시 등 |
| 완료 메시지 | 텍스트 | "등록이 완료되었어요" |
| 등록 내용 요약 | 텍스트 | 상품명, HS Code, 체적 |
| 메인으로 버튼 | 버튼(primary) | 메인 화면으로 이동 |

**API 호출**
```
POST /product/{product_id}/finalize
Response:
{
  "status": "completed",
  "finalized_at": "ISO8601 datetime"
}
```

---

### 3.7 분석 이력 화면

**진입 경로**: 메인 화면 하단 [시계] 아이콘

**UI 구성 요소**
| 요소 | 타입 | 설명 |
|---|---|---|
| 뒤로가기 버튼 | 아이콘 버튼 | 메인 화면으로 이동 |
| 이력 리스트 | 리스트(카드형) | 각 카드: 상품명, 등록일, HS Code, 상태. 클릭 시 화면 4 형태로 상세 조회 |
| 하단 네비게이션 바 | 탭바 | - |

**API 호출**
```
GET /product/history?page=1&limit=20
Response:
{
  "items": [
    {
      "product_id": "string",
      "product_name": "string",
      "hs_code": "string",
      "status": "in_progress | completed",
      "created_at": "ISO8601 datetime"
    }
  ],
  "total_count": "number"
}
```

---

### 3.8 설정 화면

**진입 경로**: 메인 화면 하단 [사람] 아이콘

**UI 구성 요소**
| 요소 | 타입 | 설명 |
|---|---|---|
| 사업자 정보 | 리스트 항목 | 클릭 시 사업자 정보 관리 화면 이동 |
| 알림 설정 | 리스트 항목 | 클릭 시 알림 설정 화면 이동 |
| 언어 설정 | 리스트 항목 | 클릭 시 언어 선택 화면 이동 |
| 고객센터 | 리스트 항목 | 클릭 시 고객센터 문의 화면 이동 |
| 하단 네비게이션 바 | 탭바 | - |
| 시연 설정 (숨김) | 섹션 | **[추가 2026-08-11]** 프로필 영역을 길게 눌러야 열린다. 각도 게이팅 on/off, 상자 자동 인식 on/off, 강제 오류 주입, 서버 주소, 운영 현황 진입. 일반 사용자에게는 노출되지 않는다 (변경 이력 4 참고)<br>**[철회 2026-08-11] '상자 자동 인식' 항목 제거** (변경 이력 5 참고) |

---

## 4. 데이터 모델

### Product (상품/화물)
```json
{
  "product_id": "string (uuid)",
  "product_name": "string",
  "status": "draft | in_progress | completed",
  "created_at": "ISO8601 datetime",
  "updated_at": "ISO8601 datetime"
}
```

### HSCodeResult
```json
{
  "hs_code": "string | null",
  "status": "approved | pre_review_recommended | review_required",
  "confidence": "number (0-1) | null",
  "reasoning": "string",
  "ingredients": [
    {"name": "string", "ratio": "number"}
  ],
  "food_type": "string",
  "storage_method": "string",
  "alternatives": [
    {"hs_code": "string", "name": "string", "confidence": "number"}
  ],
  "input_method": "ai | manual"
}
```

### CBMResult
```json
{
  "width_mm": "number",
  "depth_mm": "number",
  "height_mm": "number",
  "cbm": "number",
  "weight_kg": "number",
  "input_method": "ai | manual"
}
```

---

## 5. API 엔드포인트 전체 목록

| Method | Endpoint | 설명 |
|---|---|---|
| GET | `/product/recent?limit={n}` | 최근 등록 상품 목록 |
| POST | `/product/create` | 신규 상품 등록 |
| GET | `/product/{product_id}/marker` | CBM 측정용 마커 이미지 발급 |
| POST | `/product/{product_id}/hscode/predict` | 성분표 이미지 → HS Code 판정 |
| POST | `/product/{product_id}/hscode/manual` | HS Code 수동 입력 저장 |
| POST | `/product/{product_id}/cbm/predict` | 박스 이미지 → 체적 측정 |
| POST | `/product/{product_id}/cbm/manual` | CBM 수동 입력 저장 |
| GET | `/product/{product_id}/summary` | 통합 결과 조회 |
| POST | `/product/{product_id}/finalize` | 최종 등록 확정 |
| GET | `/product/history?page={n}&limit={n}` | 등록 이력 목록 조회 |

---

## 6. 아키텍처 설계 원칙

1. **모듈 독립성**: HS Code 판정 모듈과 CBM 측정 모듈은 서로 의존하지 않는다. 한 모듈의 API 실패가 다른 모듈의 동작에 영향을 주어서는 안 된다.
2. **AI 실패에 대한 상시 대체 경로**: AI 예측이 실패하거나 신뢰도가 기준 이하일 경우, 모든 AI 기반 화면(화면 2, 화면 3)은 반드시 수동 입력 화면으로의 전환 경로를 제공해야 한다. 수동 입력 경로가 없는 AI 기능은 존재해서는 안 된다.
3. **판정 상태 3단계 일관성**: HS Code 판정 결과는 항상 `approved`(신고 가능) / `pre_review_recommended`(사전심사 권장) / `review_required`(검토 필요) 셋 중 하나로 반환하며, 프론트엔드는 이 값에 따라 뱃지 색상(녹색/주황/빨강)을 일관되게 적용한다.
4. **입력 출처 기록**: HS Code와 CBM 결과 모두 `input_method` 필드(`ai` 또는 `manual`)를 반드시 포함하여, 이후 데이터가 AI 예측 결과인지 사용자 수동 입력인지 구분할 수 있도록 한다.

---

## 7. 변경 이력

본문의 원래 기술은 지우지 않고 남겨두었다. 아래 항목이 현재 구현 기준이며, 본문에는 `[변경 YYYY-MM-DD]` 표시로 연결해 두었다.

### 1. 촬영 각도 기준: 30~40도 → 60~70도 (2026-08-11)

**사유**. 기기 기울기를 중력의 z 성분으로 계산하면서 부호를 그대로 썼다. 카메라가 바닥을 향할 때 이 값이 음수로 들어와, 게이지의 0~90도 범위에서 30~40도가 **천장 쪽**을 가리켰다. 부호를 바로잡은 뒤 실제 촬영 자세로 확인해보니, 바닥에 놓인 박스를 서서 내려다보는 각도는 60~70도가 자연스러웠다.

**반영 범위**
- 앱: `ANGLE_MIN` / `ANGLE_MAX` 상수. 게이지 눈금·라벨·안내 문구가 이 상수를 따라간다.
- 서버: `POST /product/{id}/cbm/predict`의 `device_angle` 검증 기준 30 → 60, 기본값 35 → 65.
- 예외 안내 문구: "각도가 60도보다 낮으면 …"

### 2. 촬영 버튼 활성화 조건에 상자 인식 추가 (2026-08-11)

**사유**. 각도만으로 게이팅하면 박스가 화면에 없어도 촬영이 되어 측정 과정의 설득력이 떨어졌다.

**구현**. Expo Go에는 네이티브 CV 모듈(vision-camera 프레임 프로세서, OpenCV, MLKit)이 포함돼 있지 않아 순수 JavaScript로 윤곽 검출을 구현했다. 저해상도 프레임을 0.9초 주기로 받아 그레이스케일 축소 → 소벨 엣지 → 적응 임계값 → 연결요소 라벨링 순으로 처리하고, **면적 / 테두리 집중도 / 중앙 근접도** 세 가지로 채점해 기준을 넘으면 상자로 인정한다. 테두리 집중도는 배경의 잡텍스처를 걸러내는 핵심 지표다.

객체 분류 모델(coco-ssd 등)은 채택하지 않았다. COCO 80개 클래스에 골판지 상자에 해당하는 항목이 없어 이 용도에 맞지 않는다.

**실패 대비**. 조명이 어둡거나 바닥과 상자 색이 비슷하면 검출되지 않을 수 있다. 설정의 [상자 자동 인식]을 끄면 기존과 같이 각도 조건만으로 촬영할 수 있다.

### 3. 촬영 화면 오버레이: 마커 인식 표시 → 상자 윤곽 표시 (2026-08-11)

마커 검출은 서버 목업이 담당하므로, 촬영 화면에서는 실제로 검출한 상자 영역을 초록 사각형과 "상자" 라벨로 표시한다. 카메라 프리뷰가 화면을 꽉 채우며 잘리는 만큼을 보정해 사각형 위치를 맞춘다.

### 4. 설정 화면에 시연 설정 추가 (2026-08-11)

시연 중 예외 화면을 의도적으로 띄우기 위한 제어를 넣되, 일반 사용자에게는 보이지 않도록 프로필 영역 길게 누르기로 진입하게 했다. 각도 게이팅, 상자 자동 인식, CBM/HS Code 강제 오류, 서버 주소, 운영 현황 진입을 포함한다.

### 5. 상자 인식 기능 철회 (2026-08-11)

**변경 이력 2·3을 되돌린다.** 촬영 버튼은 다시 각도 조건만으로 활성화되고, 촬영 화면에는 각도 프레임만 표시한다. 설정의 [상자 자동 인식] 항목도 제거했다.

**사유**. 실제 촬영 환경에서 검출 성능이 시연에 쓸 수준에 못 미쳤다. 두 가지 구현을 모두 시도했다.

- **앱 내 순수 JS 검출**: Expo Go에 네이티브 CV 모듈이 없어 가로 128px에서만 처리할 수 있었다. 이 해상도에서는 실사진의 상자 경계가 바닥 질감에 묻힌다. JPEG 디코드에만 수백 ms가 들어 반응도 느렸다.
- **서버 OpenCV 검출**: 해상도 제약과 디코드 비용은 사라졌고 합성 케이스 11종을 평균 4.1ms로 통과했다. 그러나 합성 이미지는 실제 촬영의 난이도를 재현하지 못했고, 실물 조건에서 안정적이지 않았다.

근본 원인은 조명·바닥 질감·상자 색이 매번 달라지는 촬영 환경에서 고전적 윤곽 검출의 한계다. 학습 기반 검출이 필요하지만, 사전학습 모델(COCO 계열)에는 골판지 상자에 해당하는 클래스가 없어 별도 데이터 수집과 학습이 선행되어야 한다. 시연 일정 안에서 다룰 범위를 넘는다고 판단했다.

**남은 것**. 촬영 게이팅은 각도(60~70도) 조건만 사용한다. 마커 검출과 박스 모서리 추정은 기존대로 서버 목업이 담당한다. 제거한 구현은 필요하면 되살릴 수 있도록 보관해 두었다.
