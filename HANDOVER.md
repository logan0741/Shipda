# Shipda 작업 현황 및 인수인계

작성일: 2026-08-12

이 문서는 **지금까지 확정된 것, 검증된 것, 아직 검증되지 않은 것, 그리고 다음에 즉시 해야 할 일**을 정리합니다. 설치와 실행 방법은 [README.md](README.md)를, 기능 상세와 변경 이력은 [Shipda_기능명세서.md](Shipda_기능명세서.md)를 보세요.

---

## 1. 핵심 목표

수출 경험이 없는 **광주·전남 영세 농가·소상공인**이 관세사나 물류업체 없이 앱만으로 수출 신고를 준비할 수 있게 한다.

| 목표 | 내용 |
|---|---|
| HS Code 자동 판정 | 표시사항 촬영 → 원재료·식품유형·보관방법 인식 → 품목분류 코드 산출 |
| 화물 체적(CBM) 측정 | 운송장 마커를 기준자 삼아 박스 촬영 → 가로·세로·높이·체적 산출 |
| 공동물류 | 같은 권역 화물을 묶어 집하·혼재·출고까지 안내하고 물류비 절감액 제시 |

**설계 원칙 두 가지가 전체를 지배합니다.**

1. **모듈 독립성** — HS Code 판정과 CBM 측정은 서로 의존하지 않는다. 한쪽이 실패해도 다른 쪽 흐름은 진행된다.
2. **AI 실패에 대한 상시 대체 경로** — 자동 판정이 실패하거나 신뢰도가 낮으면 **반드시** 직접 입력 경로를 제공한다. 수동 경로가 없는 AI 기능은 존재하지 않는다.

이 프로젝트는 **시연용**입니다. 서버는 실제 AI 추론을 하지 않고 시나리오의 고정 결과를 돌려주는 목업입니다.

---

## 2. 한눈에 보는 현재 상태

| 영역 | 상태 |
|---|---|
| 앱 화면 | 20개 전부 작성 완료, 번들 성공 |
| 서버 엔드포인트 | 전부 동작, 스모크 테스트 통과 |
| 데이터 저장 | SQLite 이전 완료, 재시작해도 유지 |
| 로그 | 업무 로그 + 접근 로그 기록 중 |
| 실행 환경 | Node 24.19.0, Python 3.12.10, Expo SDK 54 |
| 폰 연결 | Tailscale 경유로 확인됨 |
| **버전 관리** | **커밋 안 됨 — 최우선 과제** |
| **실기기 검증** | **최근 변경 다수 미확인** |

---

## 3. 확정된 작업물

### 3.1 앱 (`app/`) — Expo SDK 54, React Native 0.81.5, TypeScript

**화면 20개**

등록 플로우 12개 — 상품 정보 입력, 운송장(마커) 준비, 박스 촬영, 체적 결과, 체적 직접입력, 모서리 보정, 표시사항 촬영, 성분 결과, 성분 직접입력, HS Code 결과, 사전심사 안내, 등록 내용 확인

공동물류 3개 — 집하 안내, 혼재 대기, 출고·정산

탭·조회 5개 — 홈, 분석 이력, 설정, 상세, 운영자 지도

> 이번 세션에서 **홈 / 분석 이력 / 설정 / 상세 / 운영자 지도 5개와 `format.ts`를 신규 작성**했습니다. 나머지는 이전 작업분입니다.

**주요 구성**

- `src/api/client.ts` — Expo 개발 서버 주소에서 PC의 IP를 뽑아 API 주소를 자동 생성. 실패 시 설정에서 수동 지정 가능
- `src/state/FlowContext.tsx` — 등록 한 건의 진행 상태(사진·치수·판정결과)를 화면 간에 전달
- `src/state/SettingsContext.tsx` — 시연 제어(각도 게이팅, 강제 오류, 서버 주소)
- `src/theme.ts` — 판정 상태 3단계(신고 가능/사전심사 권장/검토 필요)의 색을 한 곳에서 관리
- `src/components/RegionMap.tsx` — 지도 API 없이 위경도를 등장방형 투영. `cos(위도)`로 경도를 축소해 비율 보정
- `src/screens/PickupScreen.tsx` 내 `PickupMap` — 출발지·집하소 두 점만 그리는 간이 지도. 비율 보정 없음(두 점뿐이라 무영향)

### 3.2 서버 (`server/`) — FastAPI + SQLite

`app/` 서브패키지 구조입니다(`uvicorn app.main:app`로 구동). `smoke_test.py`만 패키지 밖 `server/` 루트에 남아있고 `from app import db, main`으로 불러옵니다.

| 파일 | 역할 |
|---|---|
| `app/main.py` | 엔드포인트 전체, 접근 로그 미들웨어 |
| `app/db.py` | SQLite 연결·스키마·업무/접근 로그 |
| `app/store.py` | 화물 저장소. dict를 고친 뒤 `save()`/`touch()`로 반영 |
| `app/catalog.py` | HS Code 판정 규칙 (품목별 고정 결과) |
| `app/logistics.py` | 집하·혼재·출고·운영자 데이터 |
| `smoke_test.py` | 전체 엔드포인트 + 영속성 + 로그 점검 |

**DB 테이블 3개** — `products`(화물 상태, 중첩 결과는 JSON 컬럼), `events`(업무 로그), `request_logs`(HTTP 접근 로그)

**기록되는 업무 로그** — `product.create`, `cbm.predict`(성공/실패), `cbm.confirm`, `cbm.manual`, `hscode.predict`, `hscode.revise`, `hscode.manual`, `hscode.select`, `product.finalize`, `logistics.apply`, `db.seed`

**기록 조회** — `GET /admin/logs/events`, `/admin/logs/requests`, `/admin/logs/stats`

### 3.3 문서

- `Shipda_기능명세서.md` — 원문 유지 + 변경 이력 5건 (본문 표에 `[변경]`/`[철회]` 표시로 연결)
- `README.md` — 제3자용 설치·실행 안내
- `HANDOVER.md` — 이 문서

---

## 4. 이번 세션의 결정과 근거

되돌릴 때 판단 근거가 필요하므로 남깁니다.

### 4.1 Expo SDK 57 → 54 다운그레이드

**이유**: 폰의 Expo Go가 SDK 54였습니다. App Store는 기기의 iOS 버전에서 돌아가는 최신 Expo Go만 내려주기 때문에 "최신을 받아도" 54였습니다.

**과정**: 57 → 56을 먼저 시도했으나 여전히 열리지 않아 54까지 내렸습니다. 중간에 `expo install --fix`가 잔여 패키지(`@expo/metro-runtime@57`, `react-dom@19.2.8`) 때문에 peer 충돌로 실패해, `node_modules`와 `package-lock.json`을 지우고 712개 패키지를 새로 설치했습니다.

**결과**: **앱 코드는 한 줄도 수정하지 않았습니다.** SDK 56이 자동 추가했던 `expo-status-bar` 플러그인만 app.json에서 제거했습니다(54에는 존재하지 않아 번들 실패).

**되돌리려면**: `scratchpad/sdk57-backup/`에 SDK 57 시절 `package.json`/`package-lock.json`/`app.json`이 있습니다.

### 4.2 Tailscale 경유 연결

**이유**: 이 PC에는 **무선 랜카드가 없습니다**(유선 이더넷만). 폰이 다른 공유기(192.168.219.x)에 있어 PC(192.168.0.x)에 도달할 수 없었습니다.

**방법**: `REACT_NATIVE_PACKAGER_HOSTNAME`을 Tailscale IP로 고정. 앱이 이 주소에서 IP를 뽑아 API 주소도 자동으로 만들기 때문에 별도 설정 없이 양쪽이 해결됩니다.

### 4.3 촬영 각도 30~40도 → 60~70도

**버그**: `tiltFromGravity()`가 중력의 z 성분을 그대로 썼습니다. 카메라가 바닥을 향할 때 이 값이 음수로 들어와 게이지 범위(0~90) 밖으로 밀려났고, 결과적으로 **30~40도가 천장 쪽을 가리켰습니다.** 부호를 뒤집어 해결했습니다.

**범위 변경**: 부호 수정 후 실제 자세로 확인하니, 바닥의 상자를 서서 내려다보는 각도는 60~70도가 자연스러웠습니다. 앱 상수·서버 검증 기준(30 → 60)·안내 문구를 함께 바꿨습니다.

### 4.4 상자 인식 — 도입 후 전면 철회

두 가지 구현을 만들었다가 **모두 제거**했습니다.

| 시도 | 결과 |
|---|---|
| 앱 내 순수 JS (jpeg-js + 자체 CV) | Expo Go에 네이티브 CV가 없어 가로 128px에서만 처리 가능. 실사진에서 상자 경계가 바닥 질감에 묻힘. JPEG 디코드에만 수백 ms |
| 서버 OpenCV | 해상도·디코드 문제는 해결. 합성 케이스 11종을 평균 4.1ms로 통과. 그러나 합성 이미지가 실제 촬영 난이도를 재현하지 못했고 실물에서 불안정 |

**근본 원인**: 조명·바닥 질감·상자 색이 매번 달라지는 환경에서 고전적 윤곽 검출의 한계입니다. 학습 기반 검출이 필요하지만 **COCO 계열 사전학습 모델에는 골판지 상자 클래스가 없어** 데이터 수집·학습이 선행되어야 합니다. 시연 일정 밖이라고 판단했습니다.

**현재**: 촬영 게이팅은 각도(60~70도) 조건만 사용합니다.

**보관 위치**: `scratchpad/removed-box-detection/`에 `vision.py`(OpenCV 검출기)와 `preview.ts`(좌표 변환)가 있습니다. 벤치마크 스크립트와 합성 케이스 11종도 scratchpad에 남아 있습니다.

### 4.5 인메모리 → SQLite

**이유**: 기존 `store.py`는 프로세스 메모리에만 보관해 **재시작하면 시연 중 등록한 화물이 전부 사라졌습니다.** `--reload`가 켜져 있으면 서버 파일을 고칠 때마다 초기화됐습니다.

**방법**: `db.py`를 새로 만들고 `store.py`의 함수 시그니처는 그대로 둔 채 내부만 SQL로 교체했습니다. `touch()`가 저장까지 하도록 하고, `touch()`를 부르지 않던 `finalize`·`apply_logistics`에 `save()`를 넣었습니다.

**부수 효과**: 집하 정보가 화물에 저장됩니다. 기존에는 `pickup_info()`가 상품과 무관한 고정 함수라 어느 화물로 조회해도 같은 값이 즉석 생성됐습니다. 이제 신청 시점의 배정(집하소·차량·시각·좌표)을 `logistics.pickup`에 보관하고 조회 시 저장된 값을 돌려줍니다.

### 4.6 서버를 `server/app/` 서브패키지 구조로 재편

**이유**: 같은 아이디어로 다른 팀원(logan0741)이 별도 GitHub 저장소에 FastAPI 스캐폴드를 만들어뒀습니다. 그쪽은 `uvicorn app.main:app`로 구동하는 패키지 구조였고(환경 세팅만 하고 실제 구현은 없는 빈 뼈대), 팀 관례를 맞추기 위해 우리 서버도 같은 구조로 옮겼습니다.

**방법**: `server/main.py`·`db.py`·`store.py`·`catalog.py`·`logistics.py`를 `server/app/`로 옮기고 `__init__.py`를 추가했습니다. `catalog.py`·`logistics.py`·`db.py`는 로컬 모듈을 import하지 않아 내용 변경 없이 옮겼고, `main.py`·`store.py`는 `import db` 같은 평면 import를 `from . import db` 상대 import로 바꿨습니다. `smoke_test.py`는 패키지 밖 `server/` 루트에 그대로 두고 `from app import db, main`으로 불러옵니다.

**주의해서 처리한 부분**: `db.py`의 `DB_PATH` 기본값이 `Path(__file__).with_name("shipda.db")`였는데, 이 파일이 `app/` 안으로 옮겨지면서 그대로 두면 DB 위치가 `server/shipda.db` → `server/app/shipda.db`로 조용히 바뀌어 **그동안 쌓인 시연 데이터가 고아가 될 뻔했습니다.** `Path(__file__).resolve().parent.parent / "shipda.db"`로 고쳐 기존 위치를 유지했고, 스모크 테스트로 재구조화 전후 `products` 건수가 그대로인 것을 확인했습니다.

**실행 명령 변경**: `uvicorn main:app` → `uvicorn app.main:app` (server/ 디렉터리에서 실행하는 건 동일). README.md·HANDOVER.md·API.md를 함께 갱신했습니다.

**로직 변경 없음**: 판정 규칙, API 계약, DB 스키마 전부 그대로입니다. 스모크 테스트 전체 통과로 확인했습니다.

---

## 5. 검증 상태

### 5.1 자동 검증 완료

| 항목 | 결과 |
|---|---|
| `tsc --noEmit` | exit 0 |
| iOS 번들 (`expo export`) | 성공, 약 1,013개 모듈 |
| 서버 스모크 테스트 | 전체 통과 |
| 집하 배정 영속성 | 두 번 조회 시 동일, summary에 저장 확인 |
| 업무 로그 기록 | `product.create`·`cbm.confirm`·`hscode.predict`·`logistics.apply` 확인 |
| DB 재연결 후 데이터 유지 | 확인 |
| 폰 ↔ 서버 연결 | Tailscale 경유로 앱 구동 확인 |

### 5.2 실기기 미확인 — 다음에 반드시 볼 것

**아래 항목은 코드 검증만 되었고 폰에서 눈으로 확인하지 않았습니다.**

1. **촬영 각도 60~70도가 실제로 바닥을 향하는지** — 부호 수정 후 실측하지 않았습니다. 확인 방법: 폰을 책상에 엎어놓으면 **90°**, 똑바로 세우면 **0°**가 나와야 합니다. 이 둘이 맞으면 눈금이 정확한 것입니다.
2. **UI 수정 6건** — 홈 상단 안전영역, 상태바 글자색, 키보드와 버튼 간격, 하단 안전영역, 사전심사 화면 여백 중복, 홈 카드 잘림
3. **이력·설정 탭 상단 여백** — 홈과 같은 방식으로 미리 통일해 뒀으나 확인 안 됨
4. **카메라 화면 오버레이 위치** — 각도 게이지·프레임이 제자리에 있는지
5. **SQLite 이전 후 실사용** — 폰에서 화물 등록 → 서버 재시작 → 이력 화면에 남아 있는지

---

## 6. 즉시 해결해야 할 과제

### 6.1 최우선 — 버전 관리

**작업물이 git에 하나도 커밋되어 있지 않습니다.**

- `app/`에만 git 저장소가 있고, 커밋은 Expo 템플릿의 `Initial commit` 하나뿐입니다
- `src/` 전체(화면 20개)가 untracked, `App.tsx`·`app.json`·`package.json`이 modified 상태입니다
- `server/`와 프로젝트 루트는 **저장소가 아예 없습니다**
- 현재 유일한 백업은 OneDrive 동기화입니다

프로젝트 루트에서 저장소를 새로 만들고 전체를 커밋하는 것을 권합니다. `server/shipda.db`, `server/.venv`, `app/node_modules`는 `.gitignore`에 넣어야 합니다.

### 6.2 시연 전 필수 점검

1. **Expo Go 자동 업데이트를 꺼두세요.** 스토어에서 SDK 55 이상으로 올라가면 프로젝트가 열리지 않습니다. 이 프로젝트는 SDK 54에 고정돼 있습니다.
2. **시연 당일에는 `--reload` 없이 서버를 띄우세요.** 파일 변경 시 재시작되며 진행 중 요청이 끊깁니다.
3. **Tailscale이 양쪽 모두 켜져 있는지** 확인하세요. 꺼져 있으면 연결되지 않습니다.
4. 5.2의 실기기 미확인 항목을 한 번씩 훑어주세요.

### 6.3 선택 과제

- **집하소 권역 자동 배정** — 현재 집하 정보는 광주권으로 고정입니다. `logistics.py`에 권역별 집하소 3곳(광주권·순천권·목포권) 좌표가 이미 있으므로, 출발지에서 가장 가까운 곳을 고르도록 바꿀 수 있습니다.
- **집하 지도 비율 보정** — `PickupMap`은 위도·경도를 독립적으로 정규화해 방향이 실제와 약간 다릅니다. 운영자 지도(`RegionMap`)처럼 `cos(위도)` 보정을 넣으면 두 지도의 방식이 통일됩니다. 두 점짜리 화면이라 실익은 크지 않습니다.
- **기능명세서 미반영분** — 상자 인식 철회는 변경 이력 5에 기록했습니다. 추가 변경 시 같은 방식으로 원문을 남기고 덧붙이세요.

---

## 7. 제약 조건

### 7.1 Expo Go에서 오는 제약

- **네이티브 모듈을 쓸 수 없습니다.** `react-native-vision-camera`(프레임 프로세서), `react-native-fast-opencv`, MLKit 계열은 전부 사용 불가입니다. 쓰려면 EAS 개발 빌드가 필요하고, iOS 실기기 설치에는 **Apple 개발자 계정($99)**이 필요합니다.
- **카메라 프레임 스트림 API가 없습니다.** 실시간 영상 처리를 하려면 `takePictureAsync`를 반복 호출해야 하는데, **국내 판매 아이폰은 OS가 셔터음을 강제**해서 반복 촬영이 사실상 불가능합니다.
- **SDK 버전이 폰의 Expo Go와 정확히 맞아야 합니다.**

### 7.2 환경 제약

- **PC에 무선 랜카드가 없습니다.** 유선 이더넷과 Tailscale 가상 어댑터뿐이라, 폰과 같은 Wi-Fi에 붙는 방법은 쓸 수 없습니다.
- **프로젝트가 OneDrive 폴더 안에 있습니다.** 동기화 중 파일 잠김으로 DB 쓰기가 실패할 수 있습니다. SQLite는 부수 파일(-wal/-shm)을 만들지 않도록 기본 저널 모드를 씁니다. 문제가 생기면 `SHIPDA_DB` 환경변수로 DB 위치를 OneDrive 밖으로 옮기세요.
- **Windows PowerShell 관련** — Node 설치 후 기존 터미널은 PATH를 갱신받지 못합니다(재부팅으로 해결). 실행 정책이 `Restricted`라 `npx`는 `npx.cmd`로 불러야 합니다. 긴 명령은 붙여넣기에서 줄이 섞이므로 짧게 나눠 실행하세요.

### 7.3 기능 제약

- **서버는 목업입니다.** HS Code 판정과 CBM 측정 모두 실제 추론이 아니라 시나리오의 고정 결과를 돌려줍니다. 사진은 업로드받되 저장하지 않습니다.
- **집하·혼재·출고 데이터는 고정값입니다.** 화물별로 달라지지 않습니다(집하 배정만 신청 시점에 저장됨).
- **상자 자동 인식은 없습니다.** 촬영 게이팅은 각도 조건만 사용합니다.

---

## 8. 현재 PC 환경 메모

| 항목 | 값 |
|---|---|
| Node.js | 24.19.0 (`C:\Program Files\nodejs\`) |
| npm | 11.17.0 |
| Python | 3.12.10 (`%LOCALAPPDATA%\Programs\Python\Python312`) |
| 프로젝트 | `C:\Users\zz413\OneDrive\Desktop\uk2` |
| PC 유선 IP | 192.168.0.116 |
| PC Tailscale IP | 100.115.85.81 |
| 폰 | iPhone 14 Pro (Tailscale 연결) |

`opencv-python-headless`와 `numpy`는 상자 인식 철회 후에도 venv에 설치된 채로 남아 있습니다. `requirements.txt`에서는 제거했으므로 새로 설치하는 환경에는 들어가지 않습니다.
