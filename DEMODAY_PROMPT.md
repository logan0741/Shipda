# Shipda — 데모데이 인수인계

이 문서 하나만 읽고도 이어서 작업할 수 있게 쓴 것입니다. 먼저 [Claude.md](Claude.md)를
읽으세요 — 모델을 만든 쪽에서 넘긴 문서이고, 그 아래 **설계 원칙 4가지는 바꾸면 안 됩니다.**

- 설치·실행: [README.md](README.md)
- 시연 당일 절차: [DEMO_CHECKLIST.md](DEMO_CHECKLIST.md)
- API 계약: [API.md](API.md) · 화면별 동작: [PAGES.md](PAGES.md)
- 결정 근거와 미검증 항목: [HANDOVER.md](HANDOVER.md)

---

## 1. 무엇을 만드는가

수출 경험이 없는 광주·전남 영세 농가가 관세사 없이 앱만으로 수출 신고를 준비하게
돕는 앱입니다. **사진 한 장으로 HS Code(수출 신고용 품목 번호)를 찾아줍니다.**

React Native(Expo SDK 54) 앱 + FastAPI 서버 + `model/` 판정 모델. 셋이 한 PC에서 돕니다.

```
app/      Expo 앱 (화면 20개)
server/   FastAPI + SQLite. app/catalog_ai.py 가 판정 진입점
model/    품목 분류기(32MB) · OCR · HS 통칙 · 관세율표 2,100행
```

---

## 2. 데모데이 평가항목별 현재 상태

평가 항목은 다섯입니다. 각 항목에서 **무엇이 되고, 무엇이 아직 안 되는지**를
아래에 그대로 적습니다. 심사위원이 물으면 숨기지 말고 이대로 답하는 편이 낫습니다.

### 2.1 MVP 통합구현도

| 항목 | 상태 |
|---|---|
| 앱 화면 20개 | 작성 완료, `npm run typecheck` 통과 |
| 서버 엔드포인트 24개 | 동작, 스모크 테스트 전체 통과 |
| HS Code 판정 | **실제 모델**. 목업 아님 |
| 체적(CBM) 측정 | **마커 기준 실측**. 가로·세로만. 높이는 입력 |
| 공동물류 | 출발지에 따라 집하 권역이 갈림 |
| 데이터 저장 | SQLite. 서버 재시작해도 유지 |

**이번에 막은 통합 구멍 두 개**

- 등록 화면이 출발지를 받아놓고 서버로 보내지 않았습니다. 집하 배정이 '담양/광주권'
  고정이었고 요약 화면의 '출발: 담양'도 하드코딩이었습니다. `POST /product/create`에
  `origin`을 추가해 이었습니다.
- 모서리 지정 화면이 좌표를 서버로 보내지 않았습니다. 모서리를 옮겨도 치수가
  그대로였습니다. 지금은 그 좌표로 실제 치수를 계산합니다.

### 2.2 대표 시나리오 완성도

**촬영 순서를 뒤집었습니다.** 상자를 먼저 찍고 물품을 나중에 찍던 것을, 물품 먼저로
바꿨습니다. 상자 안에 무엇이 들었는지부터 정해야 말이 되기 때문입니다.

```
운송장 → 상품/표시사항 촬영 → 성분 → HS Code → 박스 촬영 → 체적 → 요약
      → 공동물류 신청 → 집하 → 혼재 → 출고·정산
```

순서가 절반만 바뀐 채 배선이 끊겨 있던 것을 이었습니다. 그전에는 정상 경로로 가면
**체적을 재지 않고 요약으로 넘어갔고**, 저신뢰도일 때만 뜨는 [사진 다시 촬영]이
박스 촬영으로 가는 유일한 길이었습니다.

**등록 화면에서 상품명을 받지 않습니다.** 이름을 입력받는 순간 사진이 할 일이 없어지기
때문입니다(이름만으로도 세번이 나옵니다). 임시 이름 `촬영 대기 화물`로 만들고 판정이
품목을 알아내면 서버가 그 이름으로 바꿉니다. 대신 **화물 종류(원물/가공품)와 출발지**를
받습니다.

**메인 시나리오는 가공품입니다.** 떡갈비 표시사항을 찍으면:

> HS 제16류 주1 — 육류 함량 80%가 20%를 초과하므로 제16류(육류 조제품)로 분류함
> → 1602.50-9000

이름만 보면 '떡'갈비라 과자류(제19류)로 가고, 상품명만으로 판정하면 '갈비'가 원육
0201.20으로 갑니다. **원재료 함량을 읽어야 조제육류가 됩니다.** 이것이 표시사항을
찍는 이유이고, 이 앱이 검색기가 아닌 이유입니다.

### 2.3 데이터 근거 및 검증도

**판정 근거를 항상 문장으로 내려보냅니다.** `reasoning`(왜 이 호인가)과
`review_note`(무엇이 불확실한가)가 화면에 그대로 표시됩니다.

**검증한 것 — 전부 재현 가능합니다.**

```bash
cd server && python smoke_test.py     # 마지막 줄 '=== 전체 통과 ==='
```

| 검증 | 결과 |
|---|---|
| 원물 5품목 판정 | 딸기 0810.10 / 배 0808.30 / 무화과 0804.20 / 수박 0807.11 / 멜론 0807.19 |
| 가공품 6품목 판정 | 갓김치 2005.99 / 고추장 2103.90 / 배추김치 2005.99 / 배즙 2009.89 / 한과 1905.90 / 표고스낵 2003.90 |
| kind 분기 | 같은 사진도 fresh면 판정 불가, processed면 0201.20 |
| 판정 불가 | 지원 밖 품목은 `hs_code: null` + `action: manual_input` |
| 상태 선택 | 딸기 → 냉동 0811.10 후보 선택 반영, 목록에 없는 코드는 422 |
| 마커 실측 | 600×450 → 오차 2.2%/1.8%, 380×300 → 0.4%/0.1%, 900×350 → 2.2%/2.9% |
| 마커 오탐 | 민무늬 상자·검은 사각형·아주 멀리서 → 모두 `marker_not_found` |
| 표시사항 OCR | 합성 표시사항 5건 중 4건 정확 |

**모델 성능 수치**(Claude.md, 공개 데이터셋 기준): 품목 인식 Top-1 87.4%, Top-2 94.9%
(5품목 1,583장). 품목+상태 → 코드 17개 조합 전부 정답.

**아직 검증 못 한 것: 실기기 촬영 전부.** 위 숫자는 합성 이미지와 공개 데이터셋
기준입니다. 실촬영은 초점·조명·손떨림 때문에 더 나쁩니다.

### 2.4 AI 추천 성능 및 조건 반응

**조건에 따라 결과가 갈립니다** — 시연에서 보여줄 지점입니다.

| 조건 | 반응 |
|---|---|
| 화물 종류 fresh / processed | 사진 처리 경로가 갈림. 원물은 OCR을 아예 돌리지 않음 |
| 원재료 함량 | 육류 20% 초과 → 제16류. 함량이 없으면 적용 불가 → 상태 강등 |
| 원물의 상태 | 신선 0810.10 / 냉동 0811.10 / 건조 0813.40 — 사진으로 알 수 없어 후보로 제시 |
| 출발지 | 여수 → 순천권 7호차 10:40 / 해남 → 목포권 5호차 16:00 / 담양 → 광주권 3호차 14:20 |
| 마커 크기·유무 | 없으면 "운송장이 화면에 다 들어오게", 작으면 "조금 더 가까이서" |
| 촬영 각도 | 60도 미만이면 "조금 더 위에서 찍어주세요" |

**확신하지 못하면 추측하지 않습니다.** 세번을 특정하지 못하면 `hs_code: null`로 내리고
직접 입력이나 사전심사로 안내합니다. 잘못된 세번으로 신고하면 통관이 거부되어 화물이
폐기되므로, **확신에 찬 오답이 가장 위험합니다.**

모델이 '모른다'를 말하지 못하는 문제(학습한 품목 중 하나를 반드시 답함)에 대비해
방어가 셋 있습니다 — 채도 검사, 표시사항 판별, 어휘 사전 검사. 자세한 것은 API.md 6절.

### 2.5 UI 실패 대응 및 시연 안정성

**모든 AI 단계에 직접 입력 경로가 있습니다.** 수동 경로가 없는 AI 기능은 없습니다.

시연용 숨김 설정으로 실패를 원하는 타이밍에 재현할 수 있습니다 — 설정 탭에서
프로필 영역을 0.7초 길게 누르면 열립니다(표시사항 흐림, 함량 없음, 각도 미달,
마커 미인식, 모서리 가림).

**이번에 잡은 안정성 문제 세 가지**

| 문제 | 증상 | 조치 |
|---|---|---|
| 모델 지연 로딩 | 첫 가공품 판정 6.5초 — 앱이 멈춘 것처럼 보임 | 서버 startup에서 백그라운드 예열. 6.45s → 3.01s |
| cp949 콘솔 | 한국어 Windows 콘솔에서 **가공식품 판정기가 통째로 죽음** → 모든 가공품 판정 불가 | `server/app/__init__.py`에서 출력 인코딩 UTF-8 고정 |
| requirements 누락 | fastapi 3줄뿐이라 새로 설치하면 판정이 전부 불가 | 모델 런타임 의존성 추가 |

지연 로딩 문제가 지금까지 안 보였던 이유는 **DB 시드가 모델을 우연히 먼저 올려놨기
때문**입니다. 시드는 DB가 비었을 때만 도니, 데이터가 쌓인 시연 당일에는 건너뛰고
첫 요청이 전부 떠안습니다.

**시연 전 반드시 `GET /health`의 `ready`가 true인지 확인하고 시작하세요.**

---

## 3. 이번에 바꾼 것 (커밋 순서)

```
bba15c2  HS Code 판정을 목업에서 실제 모델로 교체
1d38dbe  촬영 순서를 뒤집고 등록 화면에서 상품명 입력을 제거
4b0bbb3  문서 5종을 실제 모델·새 촬영 순서에 맞춰 갱신
38c6245  표시사항 파싱이 OCR 오독에 무너지던 문제 수정
07ba665  시연 안정성: 모델 예열과 출발지 배선
27c3df7  시연 당일 체크리스트 추가
798340d  체적을 운송장 마커 기준으로 실측하도록 교체
```

각 커밋 메시지에 왜 그렇게 했는지가 적혀 있습니다. `git show <해시>`로 보세요.

### 특히 알아야 할 두 가지

**표시사항 파싱** — easyocr이 한글 뒤의 `%`를 `9`로, `료`를 `로`로 읽습니다.

```
입력  원재료명: 쇠고기 55%, 돼지고기 25%
OCR   원재로명: 쇠고기 559, 돼지고기 259
```

`원재로명`이 헤더 정규식에 안 걸려 구간을 못 찾고, `%`가 사라져 함량이 유실되면
제16류 주1을 적용할 근거가 없어져 **떡갈비가 원육으로 나갔습니다.** 헤더 정규식에
오독을 허용하고(`원재[료로]`), `및 함량`을 흡수하고, 원재료 구간에 한정해 `%`를
되돌려 고쳤습니다. 전체 텍스트에 걸면 유통기한(2026.08.19)이 망가집니다.

**체적 실측** — 마커 크기가 정확도를 가르는 가장 큰 요인이었습니다.

| 마커 | 최대 오차 |
|---|---|
| 50mm | 216% |
| 100mm | 13.6% |
| **150mm (현재)** | **2.9%** |

작은 기준자로 만든 원근 변환을 박스 전체로 확장하면 모서리 1px 오차가 멀리서 크게
벌어집니다. **운송장을 다시 인쇄해야 합니다.**

---

## 4. 바꾸면 안 되는 것

Claude.md의 설계 원칙 4가지에 더해, 이 프로젝트 전체를 지배하는 두 가지가 있습니다.

1. **모듈 독립성** — HS Code 판정과 CBM 측정은 서로 의존하지 않습니다. 한쪽이
   실패해도 다른 쪽 흐름은 진행됩니다.
2. **AI 실패에 대한 상시 대체 경로** — 자동 판정이 실패하거나 신뢰도가 낮으면
   반드시 직접 입력 경로를 제공합니다.

그리고 API 계약을 깨지 마세요. `status` 3단계, `alternatives`, `input_method`,
`force_error`는 앱이 그대로 의존합니다. 이번 모델 교체에서도 스키마는 건드리지
않았기 때문에 프론트 수정이 거의 없었습니다.

---

## 5. 남은 일

### 5.1 최우선 — 실기기 검증

**아직 폰으로 한 번도 돌려보지 않았습니다.** 확인할 것:

1. **마커 실측 오차** — 150mm 운송장을 인쇄해 박스에 붙이고, 줄자로 잰 값과 비교.
   오차가 10%를 넘으면 `MIN_MARKER_RATIO`나 마커 크기를 다시 조정해야 합니다.
2. **원물 품목 인식** — Claude.md에 "폰으로 찍은 사진에서 수박이 딸기로 나온다"고
   적혀 있습니다. 학습 데이터가 공개 데이터셋이라 배경·조명이 실촬영과 다릅니다.
3. **표시사항 OCR** — 실제 포장 뒷면에서 원재료 함량이 읽히는지.
4. **촬영 각도 게이지** — 폰을 엎으면 90도, 세우면 0도가 나와야 합니다(부호 수정 후 미확인).

### 5.2 학습 데이터가 오면 — 재학습

사용자가 품목별 폴더로 학습 데이터를 줄 예정입니다. 경로를 받으면:

```bash
python train_item_classifier.py --data <경로> --out item_model.pkl --features v2
```

**증강을 먼저 넣어야 합니다.** `model/train_item_classifier.py`의 `augment()`에
밝기·대비 변화, 약한 블러, JPEG 압축 흉내를 추가하세요. **색상(H)은 건드리면 안 됩니다
— 색이 품목을 가르는 주 신호입니다.**

기존 `model/item_model.pkl`을 백업하고 진행하세요. 새 모델이 더 나쁘면 즉시 되돌려야
합니다. 재학습 전후로 `server/check_model.py <사진경로>`를 돌려 비교하세요.

### 5.3 그 외

- **높이 다방면 예측** — 마커가 놓인 윗면 평면 밖이라 한 장으로는 잴 수 없습니다.
  여러 장에서 복원하려면 카메라 자세 추정이 필요하고, 초점거리를 모르면 그 오차가
  높이 오차가 됩니다. 실기기 사진을 몇 장 받아본 뒤 붙이는 게 안전합니다.
- **다른 화물의 운송장 경고** — 서버는 `marker.id_match`로 구분하지만 앱이 아직
  경고를 띄우지 않습니다.
- **검증 리포트 페이지** — 위 2.3의 표를 심사위원에게 보여줄 형태로.

---

## 6. 환경과 함정

| 항목 | 값 |
|---|---|
| Expo SDK | **54 고정.** Expo Go가 55 이상으로 올라가면 프로젝트가 안 열립니다 |
| Python | README는 3.12 기준. 현재 작업 PC는 3.14, `server/.venv` 없이 전역 설치 |
| 폰 연결 | Tailscale 경유. 양쪽 다 켜져 있어야 합니다 |
| 서버 실행 | `uvicorn app.main:app --host 0.0.0.0 --port 8000` (`--reload` 금지) |

**함정 셋**

- `cv2.imwrite`는 한글 경로에 **조용히 실패**합니다. 파일이 안 생깁니다.
- Windows PowerShell의 `Invoke-RestMethod`는 한글 본문을 깨뜨립니다. curl이나
  python으로 테스트하세요.
- 마커 크기를 바꿨으면 `npx expo start -c`로 캐시를 비우고 **운송장을 다시 인쇄**하세요.

---

## 7. 확인 명령

```bash
cd app && npm run typecheck          # 아무 출력 없으면 정상
cd server && python smoke_test.py    # '=== 전체 통과 ==='
cd server && python check_model.py   # 모델 적재 단계별 진단
curl http://localhost:8000/health    # ready:true 여야 시연 가능
```

작업할 때는 **한 번에 하나씩 고치고 확인**하세요. 데모가 임박했다면 새로 고치는 것보다
되돌리는 쪽(`git stash`, 마지막 정상 커밋)을 택하는 편이 낫습니다.
